<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pastebin Lite</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <h1>Pastebin Lite</h1>
        <p>Create a transient paste.</p>
        
        <form id="pasteForm">
            <div class="form-group">
                <label for="content">Content</label>
                <textarea id="content" name="content" rows="10" required placeholder="Paste your text here..."></textarea>
            </div>
            
            <div class="form-row">
                <div class="form-group">
                    <label for="ttl">Expires In (Optional)</label>
                    <select id="ttl" name="ttl">
                        <option value="">Never</option>
                        <option value="60">1 Minute</option>
                        <option value="3600">1 Hour</option>
                        <option value="86400">1 Day</option>
                        <option value="604800">1 Week</option>
                    </select>
                </div>
                
                <div class="form-group">
                    <label for="maxViews">Max Views (Optional)</label>
                    <input type="number" id="maxViews" name="maxViews" min="1" placeholder="e.g. 5">
                </div>
            </div>

            <button type="submit">Create Paste</button>
        </form>

        <div id="result" class="hidden">
            <p>Paste created successfully!</p>
            <div class="link-box">
                <a href="#" id="pasteLink" target="_blank"></a>
            </div>
        </div>
    </div>

    <script src="/script.js"></script>
</body>
</html>
