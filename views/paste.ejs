<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Paste</title>
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <div class="container">
        <h1>Paste</h1>

        <div class="paste-content">
            <pre><%= paste.content %></pre>
        </div>

        <div class="meta-info">
            <% if (paste.expiresAt) { %>
                <p><small>Expires: <%= new Date(paste.expiresAt).toLocaleString() %></small></p>
                <% } %>
                    <% if (paste.maxViews) { %>
                        <p><small>Views: <%= paste.viewCount %> / <%= paste.maxViews %></small></p>
                        <% } %>
        </div>

        <a href="/" class="btn-link">Create New Paste</a>
    </div>
</body>

</html>